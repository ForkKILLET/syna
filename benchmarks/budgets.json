{
  "$schema": "./budgets.schema.json",
  "description": "Initial v0.5 micro-benchmark budgets. Locked after the first application/benchmark run and before optimization. Values are targets for the recording machine (see results-v0.5.0.json environment), not cross-machine guarantees.",
  "lockedAt": "2026-09-04",
  "cases": {
    "warm-enter-dispose-300-depth-2": { "metric": "p95Ms", "max": 2 },
    "warm-enter-dispose-300-depth-6": { "metric": "p95Ms", "max": 2 },
    "warm-enter-dispose-100-depth-2": { "metric": "p95Ms", "max": 1 },
    "churn-10000-requests": { "metric": "planCacheEntriesMax", "max": 8 },
    "churn-10000-requests-liveEnvs": { "metric": "liveEnvCountAfter", "max": 2 },
    "lru-churn-500-shapes": { "metric": "planCacheEntries", "max": 16 }
  }
}

/goal 先完整读取工作区根目录的 SYNA_V07_EXECUTION_PROMPT.md，并按该任务书完成 Syna v0.7：删除 0.6 宣布到期的全部 23 个别名与 0.5 调用形态、清理 selector 遗骸（availability）、按表拆分 FRESH_CONSTRAINT_FAILED 与 INVALID_ENV_STATE 并收紧 INVALID_DESCRIPTOR/MISSING_IMPLEMENTATION/asSynaError 的 details、实施 S1（deadline 是等待者超时，迟到的成功在 owner 仍 ready 时被接纳，只有关闭会丢弃）与 S2（env.state 只由 Runtime 动作推进，有界关闭后为 disposed，账本与 GC 解耦，dispose 不再因用户代码不响应取消而拒绝）。任务书之外的公开名字一个不改、不加、不删；S1/S2/S6 之外的语义一字不动；规划层零变化；C.all 放松、primary()、ServiceFamily.range()、load({timeoutMs})、跨祖先复用、Prepared、世代切换宿主一律不做，留在 docs/DEFERRED.md。

Phase A 先产出 work/v07/PROPOSAL.md（S1 状态机、S2 的 dispose 返回契约方案与理由、S7 的抛出点到码的完整表、S1/S2 对现有测试的撤回清单）并停下等审阅；批准后继续。

成功结束必须同时满足：API 清单中 @deprecated 项为 0 且 gate 断言；no-old-names 模式表覆盖全部删除名与旧错误码并对应用/demos/benchmarks/scripts 零命中；S6 四个抛出点、S7 全部抛出点、INVALID_DESCRIPTOR 28 个抛出点各有测试且 details 非空、无 undefined 必填字段；S1 的四个反例场景（慢成功被接纳且未 cleanup、关闭时丢弃并 cleanup、多个等待者先后超时后接纳、eager 到期回滚）与"到期不消耗 attempts、默认值仍 30_000"有测试；S2 的 state 断言测试不使用 --expose-gc，有界关闭后 state 为 disposed 且账本受测试保护；旧 JSON key implementationId 可解析并发诊断事件，kind 判别字段不变；reference planner 差分与 explain/inspect 快照逐字不变；SEMANTIC_MODEL §11/§13、SEMANTIC_CHANGES_V07（保留/澄清/修订/撤回登记）、MIGRATION_V06_TO_V07（含错误码映射表）、API_STABILITY 的 1.0 候选声明、DEFERRED 移除已解决项、CHANGELOG 齐备；全部核心/类型/应用/scripts 测试与真实 PostgreSQL 矩阵通过；benchmark 与 0.6 同机交替对比 ±10% 且缓存计数不变；any 不增；release gate 从最终归档重新解包、安装、编译、运行必跑项并实际输出 COMPLETE。最后把 gate 真实摘要、归档哈希、清单 diff 统计与撤回测试清单输出到会话，不以"已完成"的文字代替证据。

持续执行、修复和复验，不停在规划或某个子任务通过（Phase A 的审阅停顿除外）。若仅因 PostgreSQL 不可用、依赖安装失败等确实无法自行消除的外部条件阻塞，完成其余可做部分后记录 work/v07/STATE.md 和精确复现命令，报告 BLOCKED 并暂停；BLOCKED 不是成功，任务大或测试红不构成 BLOCKED。

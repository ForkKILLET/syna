/goal 先完整读取工作区根目录的 SYNA_V08_EXECUTION_PROMPT.md，并按该任务书完成 Syna v0.8——1.0 之前最后一次改名：§2.1 类型名（EnvHandle→Env、EntryDescriptor→Entry、ImplementationDescriptor→ImplementationRecord、NodeDisposition→NodePlacement、InputType→InputValue、新增 SlotState、UniquenessPolicy 去掉 'none'）、§2.2 字段与参数名（key→id、metadata 两级显式、candidateRef/implementationRef、range 参数与 JSON 键、privateServices、planCache.limit、bindingsAssigned、placement、elapsedMs、attemptNumber、loadTimeoutMs、pinnedSlot/pinnedRevision、kind 改为 'implementation-ref'）、§2.3 值与事件（LOAD_TIMEOUT、pinned-dependency-mismatch、'all-implementations'、reused、有类型的 state、'overdue'、四个事件改名、删除 legacy-implementation-ref 事件）、§2.4 结构（derive(options)、catalog.revisions(family)）、§2.6 文档与工具修正。不设别名，不保留任何旧键或兼容分支（包括 0.6 加入的 implementationId 读取分支）；表外任何名字不改，任何语义不改，任何公开选项不加。

Phase A 产出 work/v08/RENAME_TABLE.md 与 codemod 试跑统计后直接继续，不停顿；只有发现表外必须一并改的名字或某项改名会改变行为时才停下报告。

成功结束必须同时满足：前后 API 清单 diff 的每一条都对应 RENAME_TABLE 的一行且没有表外条目；@deprecated 为 0；no-old-names 覆盖全部旧名、旧码、旧键与旧事件名并对 apps/packages/benchmarks/scripts/核心源码零命中；消费者全部经 scripts/codemod-v08.mjs 迁移且 codemod 幂等；reference planner 差分与 explain/inspect 快照按映射改写后逐字一致、limits 默认值逐字不变；ImplementationRef 只有 { kind: 'implementation-ref', contractId, familyId, range } 一种形状，旧键对象被 parse() 以 INVALID_DESCRIPTOR 拒绝并有测试，核心里 implementationId / persistent-implementation-ref / legacy 的 grep 为零；三处 state 有类型且 SlotState 与实际状态集一致；DeadlineQueue 写进 ARCHITECTURE 并有跨 Runtime 隔离与进程自然退出两条测试；inventory 工具区分 doc-only 变化；SEMANTIC_MODEL 全文措辞同步（删 strong、小写状态、pinned、load timeout、Env/Entry）；MIGRATION_V07_TO_V08 逐项、API_STABILITY 改写为"1.0 前不承诺兼容、公开面自 0.8.0 冻结、1.0 起只按 major 变化"、DEFERRED 新增"命名（2.0）"节、CHANGELOG 齐备；全部核心/类型/应用/scripts 测试与真实 PostgreSQL 矩阵通过；benchmark 与 0.7 同机交替对比 ±10% 且缓存计数不变；any 不增；README 示例测试通过；release gate 从最终归档重新解包、安装、编译、运行必跑项并实际输出 COMPLETE。最后把 gate 真实摘要、归档哈希、清单 diff 统计与 RENAME_TABLE 的对照结果输出到会话，不以"已完成"的文字代替证据；不打 tag，不推送。

持续执行、修复和复验，不停在规划或某个子任务通过。若仅因 PostgreSQL 不可用等确实无法自行消除的外部条件阻塞，完成其余可做部分后记录 work/v08/STATE.md 和精确复现命令，报告 BLOCKED 并暂停；BLOCKED 不是成功，任务大或测试红不构成 BLOCKED。

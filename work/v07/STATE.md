# Syna v0.7 — working state

Task book: `SYNA_V07_EXECUTION_PROMPT.md` (untracked at the workspace root; never committed). Baseline 0.6.0 = commit 582c93a.

## Phase A — proposal (2026-09-06) — APPROVED

- `work/v07/API_INVENTORY_BEFORE.{md,json}` regenerated on 582c93a: 387 items, 23 `@deprecated`, identical to `work/v06/API_INVENTORY_AFTER.json` except the `commit` field.
- `work/v07/PROPOSAL.md` (commit 2c2de67): deletion inventory (§1), S1 state machine (§2), S2 state + `dispose()` contract, recommendation (i) (§3), S6 table (§4), S7 tables (§5), S8 (§6), S10 (§7), permanent `implementationId` key + event (§8), snapshot impact (§9), test-withdrawal register (§10), phases/gate (§11), Q1–Q11 (§12).
- Review: the user answered "继续" — the proposal is approved with every §12 recommendation unchanged (Q1 TypeError for a call-time `scope` key; Q2 delete `AvailableImplementationCandidate`; Q3 overdue attempts listed as `timed-out`; Q4 `ENTRY_ACTIVATION_FAILED` keeps its shape; Q5 summary event `attempts-outstanding`; Q6 delete `UNSETTLED_ATTEMPT`; Q7 unreachable `INVALID_ENV_STATE` sites become internal `Error`s; Q8 `CandidateRef.revisionKey` → `INVALID_DESCRIPTOR`; Q9 `asSynaError` details = site details + `cause: { name, message }`; Q10 event `legacy-implementation-ref`; Q11 waiter window per attempt; S2 option (i); S7 codes `ENV_CLOSED` / `RUNTIME_CLOSED` / `SLOT_NOT_LOADABLE` / `LIFECYCLE_MISUSE`).

## Phase B — deletions (2026-09-06)

### B1 — §2.1: the 23 aliases and the 0.5 call form — DONE (this commit)

- Source: `descriptors.ts` (types, `EntryCallArguments` / `EntryRunCallArguments` without the scoped branches, `RuntimeEvent` gains `legacy-implementation-ref`), `index.ts` (10 exports gone), `definition.ts` (`scope` at definition time → `TypeError`; `LEGACY_FAMILY_KEY` read permanently, `parse()` marks refs from old-key documents with a non-enumerable Symbol, `isLegacyImplementationRef()` / `normalizeImplementationRef()`), `runtime.ts` (`scope` / `reuse` inside the parameter record → `TypeError`; the four nested option records → `TypeError` naming `limits.<key>`; `EnvImpl.bind` gone), `runtime-model.ts` (`PolicyContext` without `site`), `implementation-directory.ts` (`familyOf(ref, site)` emits the event once per read; `candidatesForFamily`), `entry-planner.ts` (Binding path reads through `familyOf`).
- Inventory: 364 items, 0 `@deprecated`; diff vs the 0.6.0 record = exactly the 23 items (`scripts/tests/api-inventory.test.mjs` asserts equality, not containment). `any` count 181 (baseline 204; per-file check green after dropping an explicit `<any>` from `familyOf`).
- Tests: deleted `v06-r1` … `v06-r6`, `v06-m1-limits` (registered for `SEMANTIC_CHANGES_V07` §撤回); added `v07-expired-forms.test.mjs` (12 tests: declarations, definition, call shapes, derive, anchored entries, Runtime surface, refs, policy context, locked defaults, each limit key, refused records) and `v07-legacy-implementation-key.test.mjs` (4 tests: `to()`, `parse()`, every Runtime read path + event, diagnostics-only). `type-tests/api.ts`: every expired form `@ts-expect-error` (file-level `syna-v05-compat` marker kept, since it spells them all). `deprecations.test.mjs`: empty register, asserts 0. `no-old-names.test.mjs`: 0.7 wording, exemption list = 5 files, new scan of `packages/core/src` for the deleted public names.
- Docs: `API_REFERENCE` (ref key permanent + event, `scope` refusal, `SynaRuntime` / `context.site` / nested records removed, "Deprecated in 0.6, removed in 0.7.0" collapsed to one paragraph), `API_STABILITY` (policy: empty register, permanent R5 key), `DEFERRED` N5, `PLUGIN_AUTHORING` recipe key, `packages/core/README`, `MIGRATION_V05_TO_V06` (history note: the key is permanent), new `docs/MIGRATION_V06_TO_V07.md` (§1 table of 23 + call form, §2 permanent key, §3/§4 placeholders for C/D/E, §5 steps), `CHANGELOG` 0.7.0 section started. Hyla-mini comments: the 0.5 key is permanent data compatibility, never written.
- Suites: `npm test` 185/185, `npm run typecheck` 0, `npm run test:scripts` 21/21, `npm run test:app` 122+14 / 0 failures (`work/v05/working-set.json` restored).

### B2 — §2.2: selector remnants — DONE (this commit)

- Deleted `CandidateAvailability`, `ImplementationCandidate.availability` (producer: `CandidateIndex` constructor) and `AvailableImplementationCandidate` (Q2). Inventory: 361 items, 0 deprecated; the diff vs the 0.6.0 record is the 23 alias items + these 3 (asserted exactly).
- Tests: `v07-expired-forms` gains the `C.all` candidate test (own keys = descriptor fields + `ref`; `set.load(candidate)` / `set.load(candidate.ref)` / `set.resolve(persistentRef)`) and the declaration patterns; `no-old-names` patterns for the three names (+ samples); `api-inventory` lists.
- Docs: `DEFERRED` N2 removed (N-numbering keeps its gap), `MIGRATION_V06_TO_V07` §1 paragraph, `CHANGELOG` bullet. The current docs never described the field as unavailable and contain no selector / lease / pre-check wording beyond the "removed in 0.6" history lines and Hyla-mini's own working-set leases (grep, PROPOSAL §1).
- Suites: `npm test` 186/186, `npm run typecheck` 0, `npm run test:scripts` 21/21, `npm run test:app` 122+14 / 0, any count OK.

## Phase C — diagnostics (2026-09-06)

### C1 — S6: `FRESH_CONSTRAINT_FAILED` split by throw site — DONE (this commit)

- Codes: `INACTIVE_REUSE_TARGET` (`{ constraint: 'fresh' | 'share', env, revision | family }`, both `validateScopeTargets` sites), `INVALID_INHERITED_CHOICE` (`{ site, selectedKey, candidates }`, `graph-builder.resolveChosenRevision`), `FOREIGN_CANDIDATE_REF` (`{ expectedSourceSlot, receivedSourceSlot }`, `CandidateIndex.require`). `FRESH_CONSTRAINT_FAILED` left `SynaErrorCode` (24 codes now). Messages verbatim. `solve-errors.ts` backtrackable set: the two planner codes replace the old one; `FOREIGN_CANDIDATE_REF` is a run-time throw of `set.load()` and never backtrackable (`set.resolve()` takes persistent refs only, `INVALID_DESCRIPTOR` otherwise — unchanged).
- Tests: new `v07-s6-reuse-errors.test.mjs` — every site with every details key: INACTIVE_REUSE_TARGET × {fresh, share} × {revision, family} on `check()` (reported, `check-N`), `enter()` / `run()` / `derive()` (thrown, `env-N`) and a definition-time constraint; INVALID_INHERITED_CHOICE reproduced end-to-end through a `forward()` whose target moves between the parent's plan and the child's (a cached template is not re-solved, so the observing Entry must be unplanned in that lineage); FOREIGN_CANDIDATE_REF on `load(ref)` and `load(candidate)` with the mirror-image slot ids; no `FRESH_CONSTRAINT_FAILED` string in `dist/`. `v06-snapshots`: RENAMED value → `INACTIVE_REUSE_TARGET`, new ADDED table adds `constraint: 'fresh'` to the two recorded cases (the recording is unchanged). `contracts`, `v05-planner`, `v06-t1-errors` (`CODES`, detail keys), `v07-expired-forms` updated. `api-inventory`: `DELETED_CODES_07` / `NEW_CODES_07` (one inventory item per code: the `SynaErrorCode` member), count 24. `no-old-names`: pattern for the old code.
- Docs: `API_REFERENCE` (error table rows for the three codes in alphabetical position, the `reuse` and `C.all` sentences), `API_STABILITY` M3 row, `DEFERRED` S6 row removed, `MIGRATION_V06_TO_V07` §3 S6 table, `CHANGELOG` Diagnostics paragraph.
- Suites: `npm test` 190/190, `npm run typecheck` 0, `npm run test:scripts` 21/21, `npm run test:app` 122+14 / 0, any count OK.

### C2 — S7: `INVALID_ENV_STATE` split by meaning, `INVALID_DESCRIPTOR` one shape — DONE (this commit)

- Codes: `ENV_CLOSED` (`{ env, state }` at sites 1/2/4 in `runtime.ts`; `{ env, state, slot, revision }` at sites 11–16 through `abort.ts`'s `closedError`, whose details are a factory read at throw time), `RUNTIME_CLOSED` (`{}`, site 3), `SLOT_NOT_LOADABLE` (`{ slot, revision, state: 'disposing' | 'disposed' | 'abandoned' }`, site 6), `LIFECYCLE_MISUSE` (`{ slot, revision, attempt, state }`, site 9). Sites 5/7/8/10 are `Error('Syna internal invariant: …')` (Q7). Sites 13 and 14 are one refusal (`assertOwnerUsable`): a closing owner has always aborted its signal in the same synchronous step that set `disposing`, so the signal decides and the state wording is kept for completeness. `INVALID_ENV_STATE` left `SynaErrorCode` (27 codes now); messages verbatim. Site 2 reports `state: 'disposed'` (a detached Env; exactly true once S2 makes detach and `disposed` coincide — today only a late-settlement window differs).
- `INVALID_DESCRIPTOR`: the 28 sites carry `{ descriptor, problem, site?, path? }` with the §5.2 vocabulary; the override cycle carries `path` (the key chain, cycle closed); `CandidateIndex.normalize` requires a string `revisionKey` (Q8) so `set.load()` never reaches `MISSING_IMPLEMENTATION` with `revision: undefined`.
- Tests: new `v07-s7-env-state.test.mjs` (12 tests: every reachable site, each key asserted; the abandoned-slot and pending-at-close cases tolerate today's `UNSETTLED_ATTEMPT` rejection of `dispose()` — Phase E revisits them) and `v07-s7-invalid-descriptor.test.mjs` (table of 28 rows, the throwing module asserted from the stack, the compiled sources counted: exactly 28 sites, each passing `descriptor` and `problem`; site 3 exercised on `DefinitionCompiler` directly since the planner meets the Runtime's own Entry check first). Updated: `v05-attempts`, `v05-audit-lifecycle`, `v05-review-lifecycle`, `v05-cache-cleanup`, `core`, `v05-definitions`, `v06-t1-errors` (27 codes, exact details), `v07-expired-forms`, `type-tests/api.ts`, Hyla-mini `site-manager` F-AP3-04 (`cause.code` is `ENV_CLOSED`), `api-inventory` (removed + 4 added codes, count 27), `no-old-names` (pattern + sample).
- Docs: `API_REFERENCE` (error table: four new rows, the descriptor row with the vocabulary, the two sentences), `SEMANTIC_MODEL` §anchored Entry, `API_STABILITY` kept-codes sentence, `DEFERRED` S7 row removed, `MIGRATION_V06_TO_V07` §3 S7 (both tables, key mapping), `CHANGELOG`.

### C3 — S8: `MISSING_IMPLEMENTATION` three shapes, every field required — DONE (this commit)

- Shapes: A `{ binding, implementation, version, available }` (planner Binding assignment, unchanged), B `{ contract, site }` (graph builder ×2, unchanged), C `{ contract, implementation, version, available }` (directory: the unknown-family site gains `available: []`; the version site unchanged; the `set.load()` candidate-not-in-collection site parses `family@version` from the key and lists the collection's versions of that family). The `{ revision: string | undefined }` member is gone from `SynaErrorDetails`. `CandidateIndex.normalize` also requires the key to be `family@version` (`REVISION_KEY`), so a malformed key is `INVALID_DESCRIPTOR` at S7's site 27 and site 6 never sees an empty version.
- Tests: new `v07-s8-missing-implementation.test.mjs` (six sites, exact details, the cross-Runtime CandidateRef with coinciding slot ids, no undefined field, six compiled sites); `v07-s7-invalid-descriptor` gains the malformed-key cases. The 0.5 snapshots record only the planner and the version site (both already carried `available`), so `v06-snapshots` needs no mapping.
- Docs: `API_REFERENCE` row, `DEFERRED` S8 row removed, `MIGRATION_V06_TO_V07` §3 S8, `CHANGELOG`.

### C4 — S10: `asSynaError()` — DONE (this commit)

- `errors.ts`: a `SynaError` passes through; any other value is wrapped with `details = { ...siteDetails, cause: { name, message } }` (`Error`: `name` / `message`; else `typeof` / `String()`, guarded against a throwing `toString`) and `cause` = the original value whatever its type; nothing else is read from it. Return type `SynaError | WrappedSynaError<Code>` (the intersection of PROPOSAL Q9 for the wrapped case; the pass-through keeps its own code, so the union is the honest type). The helper types are module-private; `index.ts` still does not export `asSynaError` (asserted).
- Tests: new `v07-s10-as-syna-error.test.mjs` (Error, Error subclass, an Error carrying `code` / `details` / `cause` of its own (ignored), string / number / null / undefined / symbol / object / throwing `toString`, a code without required details, `options.cause` replaced by the value, pass-through, `diagnosticFromError` of the wrapped error, no public export).
- Docs: `DEFERRED` S10 removed, `MIGRATION_V06_TO_V07` §3 S10, `CHANGELOG`. No API_REFERENCE change (no public name, no code).

## Phase D — S1 (the deadline is the waiter's timeout) — DONE (this commit)

- `materializer.ts`: `loadService()` returns a per-waiter Promise (`slot.waiters`, `SetupWaiter` in `runtime-model.ts`) whose deadline timer is armed while an attempt runs, re-armed by every new attempt of the sequence and cleared between attempts (Q11); `raceAttempt` races the raw Promise against `attempt.abandoned` / `attempt.unreachable` only (no timer). A waiter's timeout rejects that wait with `INITIALIZATION_TIMEOUT` (+ `attemptStillRunning: true`, reworded `note`), sets `attempt.overdueAt` on the first expiry, lists the attempt as `timed-out` (Q3) and emits `attempt-overdue` once. Late success under a live owner is the ordinary success path plus `late-setup-result { adopted: true }` and the ledger removal; late failure is the ordinary failure path plus `late-setup-failure`; a close discards as before (`adopted: false`). The raw Promise is held only through `attempt.rawRef` (a `WeakRef`) past the `setup()` call (G1: strongly until the attempt is listed, weakly from then on), so an overdue attempt's collected Promise ends the race as `unreachable` (cleanups, `attempt-unreachable`, then the failure policy). A cancelled waiter (`load({ signal })`) leaves with its timer; a signal aborted by the setup itself inside the first `load()` still yields `LOAD_CANCELLED`. `runtime.ts` `EnvImpl.inspect()` adds `overdueMs` while a starting slot's attempt is overdue.
- Public surface: `RuntimeEvent` gains the `attempt-overdue` member and `late-setup-result.adopted`; `EnvInspectionNode.overdueMs?` (registered in `NEW_07` of the inventory test); `SynaErrorDetails.INITIALIZATION_TIMEOUT.attemptStillRunning: true`. No new option, no code change.
- Tests: new `v07-s1-waiter-deadline.test.mjs` (the four counter-examples: slow success adopted without cleanup; owner disposed at 150 ms — discarded after the grace and inside it; two waiters then adoption; eager rollback; plus: no attempt consumed / no backoff with a failing control that re-arms the window; late failure sticky and retry-on-next-load; `AbortSignal.timeout(20)`; recovery-cooldown waiter and a dependency waiter inside a setup; a `--expose-gc` ledger/event case for the unreachable path — no state assertion). Rewritten per PROPOSAL §10 (to be registered in `SEMANTIC_CHANGES_V07.md` §撤回 in Phase F): `v05-attempts` R09, `v05-audit-lifecycle` F-PL-02 (first case) and the F-PL-04 event list (`attempt-overdue` precedes the abandonment), `v05-review-lifecycle` R-1 (late cleanup) and R-5 (the attempt gets the whole grace). Kept unchanged: `hardening`, `lifecycle`, `v05-promises` R04 (wait cycles), `v07-expired-forms` defaults lock (30_000), F-CL3-05c (S2 in Phase E).
- Docs: `API_REFERENCE` (Service option, `limits.setupDeadlineMs`, events, `inspect()` comment, lifecycle notes, `INITIALIZATION_TIMEOUT` row), `SEMANTIC_MODEL` §11, `MIGRATION_V06_TO_V07` §4 S1, `CHANGELOG`, `DEFERRED` S1 removed. `descriptors.ts` docs for both `setupDeadlineMs` options.

## Phase E — S2 (`env.state` by Runtime actions only; dispose() fulfils) — DONE (this commit)

- `runtime.ts`: `disposeEnv` sets `env.state = 'disposed'` after `detachEnv` at the end of the bounded close whatever is outstanding (the `finalized` / `markFinalized` / `finalizeEnv` machinery is gone; `EnvImpl.state` is documented as advanced only by Runtime actions); `dispose()` throws only when `errors` (cleanup failures) is non-empty. `RuntimeImpl.dispose()` keeps the ≤ grace wait for settling attempts and then emits `attempts-outstanding { attempts }` once when the ledger is non-empty (Q5); it rejects only for the roots' cleanup errors. `EnvImpl.inspect()` adds `abandonedAttempts` (`materializer.abandonedAttemptsOf(envId)`: this Env's slots' ledger entries except `timed-out`, which is an alive Env's overdue attempt and shows as `overdueMs`). `materializer.ts`: `settleSlots()` returns nothing (the report is the event), `reportAbandoned` puts the `dependencies` list on `attempt-abandoned`, `assertNoUnsettledAttempt` is the fifth internal invariant (an unsettled attempt always belongs to an abandoned slot, refused with `SLOT_NOT_LOADABLE` first). `errors.ts`: `UNSETTLED_ATTEMPT` deleted from `SynaErrorCode` and `SynaErrorDetails` (Q6 (b), approved with the proposal); `AbandonedSlot` gone.
- Public surface: `EnvInspection.abandonedAttempts` (registered in `NEW_07`), `RuntimeEvent` members `attempts-outstanding` and `attempt-abandoned.dependencies`; `SynaErrorCode['UNSETTLED_ATTEMPT']` removed (`DELETED_CODES_07`, 26 codes); `no-old-names` pattern for the code. Hyla-mini `inputs.ts` / `app.ts` docs.
- Tests: new `v07-s2-state-and-ledger.test.mjs` (PROPOSAL §3.3: stuck setup → fulfils after the grace, `disposed`, `liveEnvCount` −1, one `abandoned` entry on both ledgers, `attempt-abandoned` with `dependencies`; Promise held vs released → state unchanged over macrotasks; late settlement empties the ledger with `adopted: false` + cleanup; parent/child; `runtime.dispose()` summary once + throwing cleanup still rejects without a coded member + control with nothing outstanding; rollback past the grace; `run()` returns the result; the d.ts surface). Rewritten per PROPOSAL §10 (to be registered in `SEMANTIC_CHANGES_V07.md` §撤回 in Phase F): `v05-attempts` K08; `v05-audit-lifecycle` F-PL-01 (both), F-PL-02 (second), F-PL-04 (both); `v05-review-lifecycle` R-3 (bounded close), R-3 retention (`--expose-gc`: state assertions withdrawn, ledger/cleanups/`attempt-unreachable` kept), R-4 (the `dependencies` list on the event), R-5 (+ control); `v05-audit3` F-CL3-03 (`keptState` withdrawn), 05a, 05b, 05c, 08 (throwing cleanup); `v07-expired-forms` limits case; `v07-s7-env-state` (plain `dispose()`, five invariants); `v07-s1-waiter-deadline` (plain `dispose()`); `v06-t1-errors` codes; `v06-snapshots` ADDED `abandonedAttempts: []`; Hyla-mini `review-app` R-2/R-3 (`onDisposalError` not called, `errors` empty, `attempts-outstanding` observed through the app's diagnostics).
- Docs: `API_REFERENCE` (`limits.disposalGraceMs`, events, both `inspect()` comments, lifecycle note, error table note), `API_STABILITY` (19 kept codes + the removal), `SEMANTIC_MODEL` §13, `MIGRATION_V06_TO_V07` §3 (S2 mapping table) and §4 S2, `HYLA_MINI`, `CHANGELOG`, `DEFERRED` S2 removed.

## Phase F — documents, version 0.7.0, gate tooling — DONE (this commit)

- `docs/SEMANTIC_CHANGES_V07.md` (new: 保留 / 澄清 S7 S8 S10 / 修订 S6 S1 S2 / 撤回登记 = PROPOSAL §10 with the final dispositions / 到期删除 / 明确不做), `docs/MIGRATION_V06_TO_V07.md` (§5 steps 4–5), `docs/API_STABILITY.md` (v0.7 header; the "1.0 candidate surface" declaration; the frozen-surface paragraph says what 0.7 revised and clarified; a "Changed in 0.7.0" table), `docs/DEFERRED.md` (v0.7 header; S3/S4/S5/S9 with the updated "why still deferred"; S11 cross-ancestor reuse, S12 Prepared / barriers, S13 generation-switching host), `CHANGELOG.md` (documents-and-release paragraph), READMEs (v0.7, `verify-v07`, the documentation index), `docs/API_REFERENCE` / `ARCHITECTURE` / `HYLA_MINI` / `SEMANTIC_MODEL` headers, `docs/PACKAGE_AUTHORING` (the wait-cycle sentence under S1), `packages/core/README`.
- Version 0.7.0: root `package.json` (name `syna-v0.7-workspace`, `verify` scripts → `verify-v07`, `verify:v06` kept), `packages/core`, `packages/tsconfig`, every workspace `@syna/core` / `@syna/tsconfig` reference, `package-lock.json` (text edit, verified with `npm install --package-lock-only`: no further change). `work/v07/API_INVENTORY_AFTER.{md,json}` recorded on this source (the inventory test asserts it is current).
- Gate tooling: `scripts/verify-v07.mjs` (the 0.6 gate with: `validation/v0.7-*`, archive `syna-v0.7.0-source`, `BASELINE_COMMIT = 582c93a`, `benchmarks/results-v0.6.0-baseline-same-machine.json` (the 0.6.0 side of the final 0.6 run's same-session comparison, measured on this machine), `scripts/any-baseline-v0.6.0.json` (183, recorded from a `git archive` of 582c93a), `work/v07/API_INVENTORY_BEFORE.json`, a new must-run step `api-inventory-no-deprecated` that reads the produced inventory and fails on any `@deprecated` item, the consumer smoke reading `env.inspect().abandonedAttempts`), `scripts/benchmark-same-session.mjs` (`--baseline-label`), `scripts/validation-doc.mjs` (baseline version from the median files, the 0.7 evidence paragraph, `api-inventory-no-deprecated`), `scripts/tests/any-count.test.mjs` (0.6.0 baseline), `.github/workflows/ci.yml`, `validation/README.md`.

## Phase G — verification, release gate, evidence — DONE

### G1 — the first `--release` run and the materialization cost — DONE (this commit)

- `node scripts/verify-v07.mjs --release` on the Phase F source: PARTIAL — every step green except `benchmark-compare`: `phase-breakdown-300.materializationMs` p50 +15.6 %, p95 +20.2 % against the recorded 0.6.0 side (the 22 other timings within ±10 %, all 116 counters equal; the record drifted 23/23 the same way in both directions, so the code, not the machine). PROPOSAL §5 had named the cost (a `setTimeout` / `clearTimeout` pair per waiter where 0.6 had one timer per attempt) and the mitigation: one timer, not a relaxed tolerance.
- Measured with a harness on the benchmark's own window (a fresh runtime, the cold first `load()` of the request-scoped slot after `enter`, ~30 µs; scratchpad only, not committed): the S1 waiter cost was the timer pair (~12 µs of the window) plus, per attempt, a `WeakRef`, two race Promises with their reactions, a `delete` on the waiter and an unconditional `FinalizationRegistry.unregister`.
- `materializer.ts`: `DeadlineQueue` (process-wide) — every armed waiter in one list sorted by expiry behind a single `setTimeout` for the earliest expiry (`unref`ed while nothing is queued); a wait that settles before its deadline costs pointer writes and no timer. When the timer fires it times out the earliest due waiter and takes the next due one from a `setImmediate`, so a timeout's consequences (the rejection chain through a wait cycle) run between two due waiters exactly as Node drained microtasks between per-waiter timers — the wait-cycle diagnosis depends on that order (`v05-promises` R04, `lifecycle` structural cycles). `Attempt` is a class with one shape (`settled` created on first use); the race's early ends are one function on the attempt (`endRace('abandoned' | 'unreachable')`) instead of two Promises; `raceAttempt`'s reactions close over the resolver only — a closure mentioning the raw Promise kept it reachable through `attempt.endRace` for the attempt's life and the unreachable diagnosis never fired (`v07-s1-waiter-deadline` GC case, `v05-review-lifecycle` R-3 retention, `v05-audit3` F-CL3-03). The raw Promise is held strongly while the attempt runs off the ledger (`attempt.raw`) and swapped for the `WeakRef` by `releaseRaw` when the attempt is listed (overdue or abandoned): the retention bound is unchanged — a listed attempt holds it weakly, and its reachability alone bounds the record; `unregister` only when `watched`. `runtime-model.ts`: `SetupWaiter` carries `slot`, `attempt`, `deadlineMs`, `onDeadline` and its queue position; `SetupAttempt` has `endRace` / `raw` / `rawRef` instead of `abandoned` / `abandon` / `unreachable` / `markUnreachable`.
- Semantics: none changed (S1 as specified — the deadline is each waiter's own; the order among waiters due at the same instant was never specified). No public name, event, code or document changed (no document mentions timers).
- Harness interleaved with the 0.6.0 build (582c93a) on this machine, 200 cold rounds × 2 and a 20 000-iteration warm loop: cold p50 29.3 / 30.9 µs vs 31.9 / 32.6 µs (0.6.0), p95 52 / 50 vs 58 / 56 µs; warm 2.27 vs 2.23 µs. Suites: typecheck 0, core 233/233, scripts 21/21, app 122+14 / 0, `any` unchanged.

### G2 — release gate and evidence — DONE (run 1 in 1abe595, run 2 in this commit)

- Run 1: `node scripts/verify-v07.mjs --release` alone on 78d611a — **COMPLETE**: 40 steps, 0 failed, 870 test executions (435 distinct cases, 435 re-run in the rebuilt copy), source fingerprint `b60ba6744a98ffdc8c0dc5690309382f8dd15dd4e634e751a408c97e4d7f569b` (340 files); `gitProvenance.modified: []`, `untracked` = the four task documents (hence `dirty: true`). `benchmark-compare` (same session against 582c93a): 23/23 timings within ±10 % — `phase-breakdown-300.materializationMs` p50 −7.4 %, p95 −3.0 %; the largest delta `bound-entry-private-range-request-enter-dispose-100` p95 +7.2 % — and 116/116 counters equal; record drift (this session's 0.6.0 side vs `benchmarks/results-v0.6.0-baseline-same-machine.json`) 23/23 within ±10 %, 116/116 equal. `api-inventory-no-deprecated`: 367 items, 0 `@deprecated`. Consumer smoke: `abandoned: 0`. `docs/VALIDATION.md` regenerated from this run (`node scripts/validation-doc.mjs validation/v0.7-release docs/VALIDATION.md`) and committed together with the run's `RELEASE_MANIFEST.json` and `validation/v0.7-release/`.
- Run 2 (final, the record of reference): the same command, alone, on 1abe595 (the source containing the regenerated VALIDATION.md) — **COMPLETE**: 40 steps, 0 failed, 870 test executions (435 distinct, 435 re-run in the rebuilt copy), source fingerprint `673d76361c218f9d37c3b83b3f6bf63e54fffb8c663d189f7e327d283dc1fb53` (340 files), `modified: []`, `previousRun` = run 1 (78d611a): `sameStepList: true`, `sameTestCounts: true`, no differences. `benchmark-compare`: 23/23 within ±10 % (`materializationMs` p50 −5.0 %, p95 −3.3 %; largest delta `bound-entry-private-range…` p95 +6.6 %), 116/116 equal. Record drift (informational, not a check): this session's 0.6.0 side vs the recorded file 19/23 — the four `phase-breakdown-300` `materializationMs` / `disposeMs` timings were slower on both sides in this run (0.6.0 p50 27.2 → 36.6 µs), which is the noise the same-session alternation exists to cancel; 116/116 equal. Archives (`work/release/`, not committed): `syna-v0.7.0-source.tar.gz` 734 018 B `75356ae1c7f6beee2b7bb20b4fb8a4b9bb3a5ba0031849e37669bf9b7f409bb9`, `syna-v0.7.0-source.zip` 922 914 B `2e8b1d30fe7bfe1943b285a259d9c8c25cd1e545e8b81edae25a6f883a872b42`, `pack/syna-core-0.7.0.tgz` 112 983 B `672447e637904ef7e96b05c4735c188cb2c4cec1649ca25d185bdcad1a975116`, `pack/syna-tsconfig-0.7.0.tgz` 1 483 B `4a397269e906482cacef7d87005488afc7da5af9ee4333c8d83c33a51fb755a8` (all listed in `validation/v0.7-release/SHA256SUMS.txt`). This commit holds that run's `RELEASE_MANIFEST.json` and `validation/v0.7-release/`; `docs/VALIDATION.md` keeps quoting run 1 by design (the doc is part of the shipped source, so the run it quotes cannot be the one that shipped it).
- Inventory diff (`validation/v0.7-release/api-inventory-diff.md`, 0.6.0 record 582c93a → this source): 387 → 367 items; removed 29 = the 23 `@deprecated` aliases + `CandidateAvailability`, `AvailableImplementationCandidate`, `ImplementationCandidate.availability` + the codes `FRESH_CONSTRAINT_FAILED`, `INVALID_ENV_STATE`, `UNSETTLED_ATTEMPT`; added 9 = `EnvInspection.abandonedAttempts`, `EnvInspectionNode.overdueMs` and the seven codes `ENV_CLOSED`, `FOREIGN_CANDIDATE_REF`, `INACTIVE_REUSE_TARGET`, `INVALID_INHERITED_CHOICE`, `LIFECYCLE_MISUSE`, `RUNTIME_CLOSED`, `SLOT_NOT_LOADABLE`; changed 7 = the `contract` (two overloads) / `service` / `entry` signatures without the 0.5 name-first call form, `RuntimeEvent`, `SynaErrorCode`, `SynaErrorDetails`; `@deprecated` 23 → 0.
- Nothing pushed, nothing published. The four task documents at the workspace root stay untracked.

Reproduce the inventories: `node scripts/api-inventory.mjs --out work/v07/API_INVENTORY_BEFORE.md --json work/v07/API_INVENTORY_BEFORE.json` (on 582c93a); `node scripts/api-inventory.mjs --out work/v07/API_INVENTORY_AFTER.md --json work/v07/API_INVENTORY_AFTER.json` (on this source; `scripts/tests/api-inventory.test.mjs` asserts the committed file is current). Per-file `any`: `node scripts/any-count.mjs --check scripts/any-baseline-v0.6.0.json`.
